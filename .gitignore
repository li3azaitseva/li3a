# Служебные файлы macOS
.DS_Store

# Исходники и мастер-файлы: НЕ должны попадать на сайт.
# Это ~140 МБ ваших рабочих материалов, включая макеты клиентов.
assets/raw/
assets/img/

# Скиллы Claude Code — локальный инструмент, а не содержимое сайта.
# Раньше их отсекал .assetsignore на стороне Cloudflare. С GitHub Pages
# публикуется всё, что попало в репозиторий, поэтому держим их вне его.
# Файлы остаются на диске, происхождение и лицензии — в .claude/skills/ORIGIN.md
.claude/
